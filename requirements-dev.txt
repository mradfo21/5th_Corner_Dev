# Development/test dependencies on top of the production requirements.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# End-to-end browser testing for the standalone UI (test_standalone_e2e.py)
playwright>=1.40
pytest>=7.4
pytest-playwright>=0.4

# After installing, fetch the browser binary once with:
#   playwright install chromium

# Native window for play.py / the packaged app. Without it, play.py
# falls back to a browser tab.
pywebview>=4.4
