openai
flask
flask-cors
gunicorn
Pillow
requests
replicate
imageio-ffmpeg
opencv-python
google-genai>=1.52.0
anthropic
flask-sock
stripe>=15,<16
# On-device object detection for the SCAN tool (local_vision.py). Ships the
# TFLite runtime; the model itself is vendored in models/ so nothing is fetched
# at boot. Optional at runtime — engine falls back to the Gemini detector if
# this import fails — but installed by default because it is the default backend.
# mediapipe depends on opencv-contrib-python, which installs the same `cv2`
# package as the opencv-python pinned above. contrib is a strict superset, so the
# two coexist as long as pip resolves them to the SAME version — which it does,
# since both are released in lockstep. Listed after opencv-python deliberately.
mediapipe>=0.10
# Installer + in-place updates (updater.py, Distribution MVP M4). Must match
# the vpk CLI that packs the release (dotnet tool install -g vpk --version ...).
velopack==1.2.158
